apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-ui-deployment
spec:
  type: NodePort
  selector:
    app: booking-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080