apiVersion: v1
kind: Service
metadata:
  name: booking-ui-service
  labels:
    app: booking-ui
spec:
  type: NodePort
  selector:
    app: booking-ui
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
